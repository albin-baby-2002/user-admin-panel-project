<header>
    <nav class="navbar navbar-expand-lg   ">
        <div class="container-fluid">
            <a class="navbar-brand  fw-bolder  p-2  rounded-1 text-bg-dark" href="#">Amazon</a>
            <button class="navbar-toggler  text-light" type="button" data-bs-toggle="collapse"
                data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false"
                aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse  mt-3  w-auto" id="navbarNavDropdown">
                <ul class="navbar-nav  ms-lg-auto">

                    <li class="nav-item">
                        <a class="nav-link active  fw-bold" aria-current="page" href="/">Products</a>
                    </li>

                    <li class="nav-item">
                        <a class="nav-link fw-bold" href="#">Cart</a>
                    </li>

                    <li class="nav-item">
                        <a class="nav-link fw-bold" href="#">Orders</a>
                    </li>

                    <li class="nav-item">
                        <a class="nav-link fw-bold" href="#">Blogs</a>
                    </li>

                    <li class="dropdown ms-5 me-3">
                        <button class="btn btn-dark dropdown-toggle" type="button" data-bs-toggle="dropdown"
                            aria-expanded="false">

                            {{#if user}}
                            {{user.name}}
                            {{else }}
                            Account
                            {{/if}}
                        </button>


                        {{#if user}}
                        <ul class="dropdown-menu">
                            <li><a class="dropdown-item" href="/logout">Log Out</a></li>

                        </ul>


                        {{else }}
                        <ul class="dropdown-menu me-5 pe-5 border-0">
                            <li><a class="dropdown-item  border" href="/">Login</a></li>

                        </ul>

                        {{/if}}


                    </li>


                </ul>
            </div>
        </div>
    </nav>
</header>